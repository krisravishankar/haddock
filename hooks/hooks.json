{
  "hooks": [
    {
      "event": "Stop",
      "description": "Remind to run /haddock:done if a session is in progress",
      "handler": {
        "type": "command",
        "command": "bash hooks/session-end-reminder.sh"
      }
    }
  ]
}
